import CategoryAll from '../assets/images/CategoryAll.png';

// eslint-disable-next-line import/prefer-default-export
export const CATEGORY_ALL = {
  name: 'All',
  id: 0,
  picture: [
    {
      name: '',
      presignedUrl: CategoryAll,
    }
  ],
};
